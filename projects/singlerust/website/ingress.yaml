apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: website-https
  namespace: singlerust
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`singlerust.com`)
      kind: Rule
      services:
        - name: website
          port: 80
  tls:
    certResolver: letsencrypt
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: website-http
  namespace: singlerust
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`singlerust.com`)
      kind: Rule
      services:
        - name: website
          port: 80
      middlewares:
        - name: redirect-to-https
